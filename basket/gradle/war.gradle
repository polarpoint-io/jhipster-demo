apply plugin: "war"

bootWar {
    mainClassName = "io.polarpoint.basket.BasketApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
